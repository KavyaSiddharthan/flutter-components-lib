import 'package:flutter/material.dart';

class LiquidButton extends StatelessWidget {
  const LiquidButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implement advanced LiquidButton physics and rendering
      child: const Center(
        child: Text('LiquidButton Placeholder'),
      ),
    );
  }
}
