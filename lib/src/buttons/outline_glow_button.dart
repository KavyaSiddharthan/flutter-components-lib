import 'package:flutter/material.dart';

class OutlineGlowButton extends StatelessWidget {
  const OutlineGlowButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implement advanced OutlineGlowButton physics and rendering
      child: const Center(
        child: Text('OutlineGlowButton Placeholder'),
      ),
    );
  }
}
