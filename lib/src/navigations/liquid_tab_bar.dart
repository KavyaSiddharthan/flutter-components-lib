import 'package:flutter/material.dart';

class LiquidTabBar extends StatelessWidget {
  const LiquidTabBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: Implement advanced LiquidTabBar physics and rendering
      child: const Center(
        child: Text('LiquidTabBar Placeholder'),
      ),
    );
  }
}
